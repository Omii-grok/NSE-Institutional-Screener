[tool.poetry]
name = "nse-institutional-screener"
version = "3.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

# ── DigitalOcean App Platform config ──────────────────
# In DO App Platform, set:
#   Run Command: streamlit run screener.py --server.port $PORT --server.address 0.0.0.0 --server.headless true
#   Environment: Python 3.11
